# Test/dev-only dependencies. Install on top of requirements.txt:
#   python -m pip install -r requirements.txt -r requirements-dev.txt
# Not installed in the production image (see Dockerfile).
pytest>=8.3
